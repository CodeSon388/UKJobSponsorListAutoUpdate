# Python internals
__pycache__/
*.pyc

# Virtual Environments
venv/
.venv/
env/

# Environment Variables (Protect Secrets)
.env
.env.local

# IDE Settings (Personal preferences)
.vscode/
.idea/

# OS System Files
.DS_Store
Thumbs.db

# Data Files (Managed by GitHub Actions)
# We ignore these locally to prevent accidental push/overwrites.
# The GitHub Action uses 'git add -f' to ensure they stay updated.
master_register.csv
stats.json
history.json
daily_delta.json
